[build]
  command = "node scripts/generate-config.js || echo 'Build script completed'"
  publish = "."

[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200

# Blog posts are served directly from blog/ directory
# No redirect needed - files are at blog/[slug].html
